comp
ghc -odir temp -hidir temp -o ${o} ${src} -Wno-x-partial

{-# LANGUAGE LambdaCase, ViewPatterns #-}

import System.IO (readFile, hFlush, stdout)
import System.Environment (getArgs)

import Data.List
import Data.Tuple
import Data.Maybe
import Data.Function
import Data.Bool (bool)
import Control.Monad
import Control.Arrow ((***), (&&&))

${content}

${main} :: String -> String

main :: IO ()
main = getArgs >>= readFile . head >>= putStrLn . ${main} >> hFlush stdout