comp
g++ ${src} -o ${o} -std=c++20

#include <iostream>
#include <fstream>
#include <filesystem>

#include <vector>
#include <queue>
#include <tuple>
#include <functional>
#include <unordered_map>

#include <algorithm>
#include <numeric>
#include <cmath>

${content}

int main() {
    std::cout.setf(std::ios_base::unitbuf);
    std::string files[] = { ${inputs.map(x => `"${x}"`).join(', ')} };
    for (auto path : files) {
        auto size = std::filesystem::file_size(path);
        std::string content(size, '\0');
        std::ifstream file(path);
        file.read(&content[0], size);
        file.close();
        std::cout << "\n" << ${main}(content) << "\n";
    }
    return 0;
}